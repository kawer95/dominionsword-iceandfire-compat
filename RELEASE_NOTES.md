# 1.1.0

- 分层飞控：粗网格 3D 航路规划、前视制导、走廊避障采样、曲率受限制导与持续速度控制；原生 FlightMoveHelper/随机目标/悬停垂直写入在受控期间被接管，移动不再破坏地形。
- 普通攻击默认进入悬停龙息（HOVER_ATTACK），在目标侧上方阻尼悬停并朝向误差 ≤25° 时吐息。
- 新增 AOE 技能：盘旋龙息（orbit_breath，24/16、无冷却）与掠袭龙息（strafe_breath，16/16、冷却 900 tick，持久化），依赖 Dominion Sword 1.28.0 的 AOE 技能 API。
- 多龙分离力、盘旋相位/方向按 UUID 稳定分配，掠袭使用平行航道。
- 无安全航路时渐进刹车进入紧急悬停；起飞验证上升走廊，降落使用有限窗口与完整碰撞箱校验。

# 1.0.1

- 修复 `move()` 每 tick 重置状态机的问题：目标未变化时不再停止导航、重置阶段或重复推进；在线路径只由 ServerTick 驱动一次，离线持久任务仍由 `move(null, ...)` 脉冲推进。
- 攻击命令幂等化，同一目标不再每 tick 重复写入与 `setTarget`。
- 重写降落点搜索：垂直窗口 ±24、Heightmap 取表面、完整龙碰撞箱校验、排除流体/火焰、64 候选/512 读取预算、失败 40 tick 冷却。
- 用维度 `getMinBuildHeight`/`getMaxBuildHeight` 替代 Y=128 飞行上限，Y=200 区域可正常起降。
- 指定骑手 UUID 通过 `SynchedEntityData` 同步到客户端，NPC 骑手不再被客户端当作嘴中猎物。
- 地面寻路失败增加退避与连续失败升级（转飞行或清目标）。
- 兼容状态全部迁入 `dominionsword_iaf` 子标签，统一清理语义与字段所有权文档。
- 固定 Mixin Gradle 插件版本 0.7.38，启动时记录检测到的 iceandfire 版本。
- 新增 JUnit 5 单元测试（飞行数学、降落搜索预算/环生成、状态清理语义）。

# 1.0.0

- 龙作为可自驱载具接入 Dominion Sword 车辆系统，支持空龙自驱、载人地面与载人空中三种形态。
- 内置三种控制权限：只认驯服主人（默认）、只看是否驯服、所有龙均可控制；通过 `DragonControlModeSource` 预留本体配置接入点。
- 载人龙右键菜单提供起飞、降落、自动控制；自动控制开启后起飞/降落灰色禁用并由服务端复验。
- 自动控制按距离决定地面/空中导航：48 格起飞、24 格降落，带迟滞避免反复起降。
- Mixin 修正普通单位骑乘时的乘客定位，避免被当作嘴中猎物；控制中的龙抑制非命令目标抢占和随机起降。
