# 龙飞控二次审计与优化记录

## 已确认问题

- 原有的降落 Y 偏移、路径游标未推进、远目标每 20 tick 重规划、紧急计数每 tick 被抵消、离线龙息 fail-open、注册表全表扫描、TAKEOFF 被 CRUISE 覆盖均已确认。
- 额外确认：避障仅覆盖普通移动；悬停、盘旋、掠袭和起降会直接写速度；视觉转向与真实速度脱节；掠袭不触发龙息；盘旋在小步长 PD 下会失速；AOE 查询是方盒而非圆柱。
- 原测试并不可信：旧 Gradle/JDK 组合会出现测试类或 Worker 的类加载失败。构建必须使用 JDK 17 与完整重新执行的任务。

## 本次实现

### 飞控内核

- 使用服务端运行时状态保存速度、航路游标、悬停锚点、掠袭阶段与路径搜索；稳态飞行不再持续写入 NBT phase。
- 路径规划替换为主线程增量 3D A*：全服每 tick 最多 1024 节点、单龙最多 128 节点、单任务最多 4096 节点，并使用 3ms 时间预算。
- 网格键改为 `Cell(x,y,z)`；规划检查世界边界、完整 AABB、所有覆盖区块已加载和碰撞，缓存上限 8192 项。
- 路径跟踪推进 `pathIndex`，仅在目标变化、明显偏航、持续停滞或路径不存在时重新搜索。
- 所有飞行模式统一进入局部安全制导：最多 9 个候选方向、每个最多 6 个 AABB 样本；无安全走廊时只渐进制动，不反向覆盖速度。
- 运动积分以真实速度为基准，限制加速度 `0.06`、制动 `0.10`、偏航 `3–6°/tick`、俯仰 `3°/tick`，保证模型朝向与 `deltaMovement` 一致。
- 注册表在服务器 END tick 统一维护；移除跨维度分离力与每龙 HashMap 快照。

### 起降与作战

- `Level#getHeight` 的返回值直接作为龙脚 Y，地面使用 `Y-1`；落地点额外检查完整体积的火、流体和中心/四角支撑。
- 起飞、降落、悬停、盘旋与掠袭都走统一安全层；落地先飞往落点上方的进近位置。
- 悬停保存稳定锚点，目标横向重合时不翻转；任务切换、离线、目标失效时立即关闭龙息。
- 盘旋使用切向速度与径向误差校正，飞行高度为 `中心Y + 10 + 2×stage`；索敌严格按水平圆柱判定。
- 掠袭包含进场、航过、脱离与超时保护；航过圆柱时会触发原生龙息，脱离后进入安全区域悬停。

### 权限、AOE 与构建

- 飞机指令和技能入口重新检查控制者、维度、有限坐标、持久任务和敌对目标；离线状态停止攻击。
- AOE `SkillArea` 拒绝 NaN/Infinity；多选施法使用所有施法者共同的最小半径和半高重建服务器范围。
- Gradle 升级到 8.14.4，写入官方分发 SHA-256，移除仓库级证书校验绕过；模组版本更新到 1.2.0。
- 兼容范围固定为 Minecraft 1.20.1、Forge 47.x、Ice and Fire 1.2.7、Dominion Sword 1.28.1。

## 验收基线

- JDK 17 下执行 `clean test build`；本机证书问题只能通过临时 `GRADLE_OPTS` 处理，禁止提交全局跳过校验。
- 覆盖路径推进、速度/角速度连续性、圆柱 AOE、权限失败零状态变化、起降回归和无解空间 100 tick 紧急悬停。
- 运行期验证 Mixin 注入、专用服务器与客户端启动，并在 1/10/25/50 条龙场景记录规划次数、避障检查与 MSPT。
