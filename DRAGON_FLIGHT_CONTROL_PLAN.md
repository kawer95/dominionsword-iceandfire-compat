# 龙飞控与 AOE 技能系统重构计划

## Summary

- 将现有“不断设置离散飞行目标”的逻辑替换为分层飞控：低频 3D 航路规划、高频局部避障、曲率受限制导和持续速度控制。
- 仅接管 Dominion 控制中的 Ice and Fire 龙；原生、野生或玩家直接骑乘的龙保持原行为。
- 默认空战采用悬停龙息；盘旋为持续 AOE 模式；掠袭为单次 AOE 龙息航过，冷却 900 tick。
- 飞控始终绕开碰撞方块，不使用 Ice and Fire 的卡住破坏方块逻辑。

## Dominion Sword 本体 API

- 在 `DominionSkillProvider` 增加 `SkillType.AOE`、`AoeSpec(radius, halfHeight)` 和 `SkillArea(center, radius, halfHeight)`：
  - AOE 使用水平圆柱；半径和垂直半高限制为 `1–64`。
  - `SkillView` 携带 `AoeSpec`；保留全部旧构造器，保证现有插件二进制兼容。
  - `SkillTarget` 增加可空的 `area`；旧 `(Vec3, Entity)` 构造器继续存在。
  - 客户端只发送中心坐标，服务器根据对应 `SkillView` 重建可信 `SkillArea`，不接受客户端半径。
- AOE API 只提供范围，不解析或返回目标：
  - 技能提供者自行决定立即查询、锁定快照或持续扫描。
  - 抽出公开的 `DominionTargeting.isHostileCandidate()` 判定，复用 Ctrl 攻击框选的控制权、同阵营、同控制者和不可选实体规则。
  - 本体不替插件结算伤害；提供者查询范围、调用敌我判定后自行攻击或施加效果。
- 多选时允许共同的 `AOE` 技能显示和释放；各施法者独立检查冷却与施法距离，同一个服务器确认中心传给所有成功施法者。
- 技能栏数据包增加 AOE 参数，网络协议由 `10` 升为 `11`。瞄准期间在鼠标地面位置渲染半径圈；超出所有施法者距离时显示禁用颜色。
- 修正多选技能合并：AOE 规格不一致时采用最小半径、最小垂直半高，避免客户端预览大于实际效果。

## 分层飞控实现

- 将高层状态统一为 `TAKEOFF`、`TRANSIT`、`HOVER_ATTACK`、`ORBIT`、`STRAFE_APPROACH`、`STRAFE_RUN`、`STRAFE_EGRESS`、`LANDING`、`EMERGENCY_HOVER`。
  - 任务目标、机动模式、AOE 范围和掠袭冷却持久化到龙 NBT。
  - 航路、控制器积分和局部避障缓存只保存在服务端运行时，实体重载后重新规划。
  - Dominion 的重复 `move()` pulse 只推进任务；目标实际变化时才初始化状态和重规划。
- 通过 `FlightMoveHelper` Mixin 仅在龙处于 Dominion 控制时取消原生“碰撞后瞬间转 180°”逻辑，由新控制器唯一写入速度、偏航和俯仰；删除兼容模组与主模组的重复 tick 推进。
- 航路层使用增量粗网格 3D A*：
  - 网格尺寸依据龙的碰撞箱确定，最低 4 格；检查节点和边的完整移动 AABB。
  - 26 邻接，代价包含距离、爬升、转向、障碍余量；规划完成后做安全直线裁剪。
  - 全服每 tick 最多展开 4096 个节点，单龙单次配额 1024；未完成的搜索跨 tick 延续。
  - 不加载未加载区块；未加载区域视为暂时不可通行。
  - 目标变化超过 4 格、偏离航路超过 8 格或前方走廊失效才重规划，正常情况下至少间隔 20 tick。
- 制导层采用前视追踪而非逐点抵达：
  - 沿平滑后的航路选择随速度变化的前视点，经过路点投影后直接推进，不停车对点。
  - 每 tick 最多评估 9 条候选局部走廊、每条最多 6 个 AABB 样本，按目标推进、净空、转向量和高度变化评分。
  - 当前走廊被阻挡时提前减速并选择侧向/上方走廊；无安全候选时渐进刹车进入悬停，不允许瞬间掉头。
  - 多龙加入预测分离力；盘旋按稳定相位错开，掠袭按龙宽分配平行航道。
- 运动控制默认参数：
  - 巡航速度 `0.8 block/tick`，战斗速度 `0.55`，掠袭速度 `1.1`，再乘 Ice and Fire 飞行速度倍率。
  - 加速度 `0.06 block/tick²`，制动 `0.10`。
  - 最大偏航速率为 `6 - 0.5 × dragonStage` 度/tick，最低 `3°/tick`；最大俯仰变化 `3°/tick`，俯仰限制 `±35°`。
  - 转角增大时按预计转弯半径提前降速；禁止直接覆盖为反向速度。
- 起飞、降落和失败处理：
  - 起飞先验证完整碰撞箱的上升走廊。
  - 降落只在目标 Y 附近有限窗口搜索，并以完整 AABB 验证，不再从世界顶部扫描。
  - 高度边界取当前维度构建高度并保留 16 格余量，不使用 Y=128。
  - 连续 100 tick 无法获得安全航路时保持紧急悬停；在线指挥者收到任务受阻提示，离线任务等待环境变化后低频重试。
  - Dominion 控制期间拦截移动造成的 `breakBlocks`；龙息是否破坏地形仍遵循 Ice and Fire 原配置。

## 战斗与技能行为

- 普通攻击进入 `HOVER_ATTACK`：
  - 非幼龙自动起飞，在目标外约 `20 + 龙碰撞箱半宽` 的位置选择安全悬停点，高度为目标上方 `10 + 2 × stage`。
  - 使用位置误差和当前速度进行阻尼悬停；目标移动或视线丢失时沿安全环重新定位。
  - 只有目标存活、敌对、存在视线且龙头朝向误差不超过 `25°` 时调用该龙种原生 `stimulateFire()`。
- 盘旋技能 `dominionsword_iceandfire_compat:orbit_breath`：
  - AOE 半径 24、垂直半高 16、最大施放距离 96、无冷却。
  - 龙在范围外侧增加至少 12 格安全边距后持续盘旋；没有敌人也保持模式。
  - 每 10 tick 查询一次圆柱内单位，使用本体公开敌我判定，保留仍有效的当前目标，否则选取最近且有视线的敌人。
  - 多龙按 UUID 稳定分配相位和顺/逆时针方向。
  - 任意新的移动、攻击、降落、释放控制或其他机动技能终止盘旋。
- 掠袭技能 `dominionsword_iceandfire_compat:strafe_breath`：
  - AOE 半径 16、垂直半高 16、最大施放距离 96，冷却 900 tick，并持久化冷却截止 tick。
  - 从龙到范围中心确定初始攻击轴；若走廊不安全，按 `±30°、±60°、90°` 搜索替代轴。
  - 完成进场、对正、单次龙息航过和至少 32 格脱离；只攻击范围内且当前可见的敌对单位。
  - 多龙使用平行错位航道。脱离完成后转入该范围附近的默认悬停攻击。
  - 技能成功进入进场状态时开始冷却；规划失败不消耗冷却。
- 技能启动通过 `PlayerControl.redirectVehicleMove()` 将持续任务中心设为 AOE 中心；相同中心的主模组 pulse 不取消技能，新命令改变目标或进入攻击接口时取消当前机动。

## Test Plan and Acceptance

- 本体测试：
  - 旧技能提供者和 Pomkots 兼容模组无需源码修改即可编译、加载。
  - AOE 数据包往返、非法半径/NaN/越界中心、服务器重建范围、多选部分冷却和协议不匹配。
  - Ctrl 框选与公开敌我判定对同一实体给出一致结果。
  - 客户端范围圈半径、施法距离颜色和多施法者预览正确。
- 飞控自动测试：
  - 直线、90° 山体、山谷、U 形障碍、洞穴、下界顶部、未加载区块和无解空间。
  - 航路不会穿过龙的完整碰撞箱；节点预算和局部探测次数不超限。
  - 转弯期间速度连续、偏航不超上限、路点处不归零、碰撞时不瞬间反向。
  - 普通悬停、持续盘旋动态索敌、单次掠袭、目标死亡、技能互相打断和区块重载。
- 实机验收：
  - 1、4、12 条龙分别连续飞行 5 分钟，记录 MSPT、规划节点数、重规划次数和紧急悬停次数。
  - 专用服务器验证客户端乘员位置、AOE 圈、龙息朝向以及技能冷却同步。
  - 在山地和建筑群中录制路径，确认龙会提前转向形成连续曲线，不发生逐点刹停或魔法掉头。
- 同步更新 `OPTIMIZATION.md`，记录新架构、默认参数、性能预算和仍未包含的工作；自定义翻滚动画及非 Dominion 龙的行为修改不在本次范围内。
